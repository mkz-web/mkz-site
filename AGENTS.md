<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes: APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# Site bilingue : RÈGLE ABSOLUE

**Tout nouveau contenu se fait en français ET en anglais.** Décidé le 30/07/2026 à la création de la version anglaise. Une page publiée dans une seule langue est un travail inachevé, pas une étape.

- **L'anglais n'est jamais une traduction du français.** Les deux versions répondent à des demandes différentes, mesurées séparément. Le français vise la demande locale (artisans, commerçants, TPE d'Île-de-France) ; l'anglais vise les entreprises étrangères qui ont besoin que le marché français fonctionne, plus la visibilité dans les moteurs de réponse IA. Avant d'écrire une page anglaise, **mesurer les volumes anglais** (DataForSEO) : si l'intention n'existe pas en anglais, ne pas créer la page et écrire pourquoi dans l'en-tête du fichier de contenu (voir `src/content/en/pillars/website-design.ts`, qui documente 10 recherches/mois et assume d'être une page de conversion, pas une page SEO).
- **Architecture** : URLs françaises inchangées à la racine, anglais sous `/en/`. Deux root layouts via route groups `src/app/(fr)/` et `src/app/(en)/`, seul moyen d'avoir deux attributs `lang` réels avec `output: "export"` (le pattern `[lang]` documenté par Next exige un proxy, impossible en export statique, et déplacerait les URLs indexées).
- **Ajouter une paire de pages** : créer les deux pages, puis déclarer la paire dans `pagePairs` (`src/lib/i18n.ts`). C'est cette liste qui produit les `hreflang` du `<head>` ET du sitemap. Une page sans équivalent (ex. `/agence-web-77/`, `/services/`, `/tarifs/`) ne déclare pas d'alternative : c'est un choix mesuré, pas un oubli (pour `/tarifs/`, 8 requêtes anglaises « pricing/cost france » à zéro volume, DataForSEO 20/08/2026 ; détail en tête de `src/app/(fr)/tarifs/page.tsx`). ⚠️ `/tarifs/` publie la grille de prix : tout changement de prix se répercute aussi dans son JSON-LD OfferCatalog, sa meta description, `llms.txt`, `llms-full.txt` et la carte tarifs de `/services/` (source de vérité : plaquette `_tarifs/` + mémoire projet).
- **Textes d'interface** : jamais en dur dans un composant, toujours dans le dictionnaire `ui` de `src/lib/i18n.ts`. Les composants de gabarit (`PillarContent`, `ArticleRenderer`, `ConseilsContent`, `CategoryContent`, `ContactForm`, `Header`, `Footer`) prennent une prop `locale`.
- **Titles** : ne JAMAIS écrire « | MKZ » dans un `title` de page, le template du layout l'ajoute. Seule exception : `src/app/(fr)/page.tsx`, car une page du même segment que son layout n'hérite pas du template Next (sinon le suffixe apparaît deux fois).
- **Articles anglais** : `src/content/en/articles/*.ts` avec `locale: "en"`, registre `_registry.ts` tenu **à la main** (`ingest-content.mjs` ne génère que le français). Cocons anglais : `french-seo` et `ai-search` (`src/lib/articles/categories-en.ts`).
- **Validation** : `node scripts/validate-out.mjs` contrôle aussi la cohérence `lang`, la réciprocité hreflang, la présence de `x-default` et l'existence réelle de chaque cible d'alternative. Un hreflang non réciproque est ignoré par Google, qui choisit alors lui-même la version à servir.

# Newsroom /conseils/ (cocons sémantiques)

- **Contenu** : les articles vivent dans `src/content/articles/*.ts` (un fichier par article, typé `Article`), générés par `node scripts/ingest-content.mjs` depuis `_content-staging/*.json` (dossier gitignoré). Le registre `src/content/articles/_registry.ts` est généré, ne pas l'éditer à la main.
- **4 cocons** (`src/lib/articles/categories.ts`) : `tutoriels` (valeur gratuite clients), `creation-site-internet`, `seo`, et `referencement-ia` depuis le 07/08/2026 (section dédiée plus bas). Chaque cocon pousse vers sa page pilier : `/creation-site-internet/`, `/referencement-seo/` (+ hub local `/agence-web-77/`), `/referencement-ia/`.
- **Captures d'écran (08/08/2026)** : un bloc `{"type":"screenshot"}` sans `src` affiche un cadre légendé « Capture d'écran à venir », l'emplacement réservé. **Les 3 tutoriels n'en ont plus aucun** : leurs 17 captures existent, et les 4 blocs restés sans image y ont été supprimés plutôt que laissés en cadre vide. Les articles de fond (`audit-seo`, `referencement-local`, `refonte-site-internet`, `seo-artisan`, `site-vitrine`) gardent leurs 6 cadres, en attente des captures. Pour ajouter une capture : convertir le PNG avec `node scripts/convertir-captures.mjs --dest=public/images/conseils --largeur-max=1400 --qualite=82 "source.png=nom.webp"` (encodage WebP par le canvas d'Edge en headless, zéro dépendance), puis renseigner `"src"`, `"width"` et `"height"` (dimensions réelles, sans quoi la page saute au chargement) dans `src/content/articles/<slug>.ts`, et mettre à jour `dateModified`. **Écrire la légende d'après l'image, pas d'après le texte attendu** : 7 légendes sur 17 décrivaient un écran différent de la capture fournie (libellés de boutons Google et WordPress qui ont changé depuis).
- **Généré au build (jamais à la main)** : `sitemap.xml` (`src/app/sitemap.ts`), `llms.txt` et `llms-full.txt` (route handlers `src/app/llms*.txt/route.ts`), tous alimentés par le registre d'articles. `robots.txt` reste dans `public/`.
- **Validation avant déploiement** : `node scripts/validate-out.mjs` après `next build` : reparse tous les JSON-LD de `out/` (règles ItemList/BreadcrumbList GSC), vérifie titles ≤ 65 / metas ≤ 165 et le maillage interne. À lancer systématiquement.
- **Nouvel article** : JSON conforme à `_content-staging/SPEC.md` → `node scripts/ingest-content.mjs` → build → validate → deploy → soumettre le sitemap GSC + inspecter l'URL.
- **Barre « Résumer avec l'IA » (07/08/2026)** : `src/components/article/AiSummaryBar.tsx`, rendue par `ArticleRenderer` entre le TL;DR et le sommaire, donc sur les articles et **nulle part ailleurs** (ni hubs, ni catégories, ni piliers, ni pages légales). Recomptage du 08/08/2026 : **18 pages la portent sur les 47 de `out/`**, soit 15 articles FR et 3 EN. Le chiffre bouge à chaque publication : le recompter (`grep -rl "chatgpt.com/?q=" out --include="*.html"`) plutôt que reprendre celui écrit ici. Quatre liens profonds `nofollow` qui ouvrent Claude, ChatGPT, Perplexity ou Mistral avec une invite pré-remplie contenant l'URL canonique : service au lecteur pressé, et GEO (chaque clic fait lire l'URL par un moteur IA). Libellés et invite dans `ui[locale].article.aiBar`, l'invite anglaise n'étant pas la traduction de la française. **La barre est du chrome, pas du contenu** : elle ne passe pas par les blocs d'article, donc ni par `llms.txt` ni par `llms-full.txt` (vérifié). ⚠️ Aucun de ces patterns d'URL n'est documenté par son éditeur : **les re-mesurer en navigateur RÉEL avant chaque déploiement**, jamais en curl (bot-walls). État du 07/08/2026 : **les quatre patterns sont vérifiés**, invite intacte à l'arrivée (URL canonique, apostrophe, accents, deux-points). **Mistral** et **Perplexity** exécutent la requête sans compte ; **ChatGPT** envoie et répond ; **Claude** pré-remplit le composer (vérifié depuis la session de Mickaël). La puce **Mode IA de Google** (`google.com/search?udm=50&q=`) a été retirée le 07/08/2026 sur décision de Mickaël, alors qu'elle était mesurée verte : le pattern reste noté dans le composant si on veut la remettre, et surtout PAS `gemini.google.com/app?q=`, qui est mort (composer vide). ⚠️ **Les deux conditions ChatGPT, isolées une à une après un « KO chez moi » de Mickaël** : (1) `?q=` **SEUL**, jamais `hints=search` que recommande le skill, car ChatGPT le consomme, réécrit l'URL pour l'en retirer et emporte `q` au passage ; (2) `rel` contenant **`noreferrer`**, car avec un referrer tiers ChatGPT REFUSE de soumettre (garde anti-abus : sans elle, n'importe quel site posterait des invites dans le compte de son visiteur). D'où un `rel` par assistant dans le composant : ChatGPT en `noopener noreferrer nofollow`, les trois autres en `noopener nofollow`, valeur sous laquelle ils ont été mesurés verts. **Symptôme à reconnaître** : la page d'accueil ChatGPT au lieu du composer rempli. Le mode Recherche était de toute façon inutile, ChatGPT ouvre l'URL de lui-même. ⚠️ **Piège de mesure, payé deux fois ici** : sans session ouverte, Claude et ChatGPT renvoient leur mur de connexion, et un navigateur automatisé se fait bloquer là où un humain passe. **Ne jamais conclure de là qu'un bouton est mort**, et ne jamais valider un pattern depuis une navigation interne au domaine de l'assistant (un `reload` ou un lien interne réussissent là où un clic depuis le site échoue) : juger en cliquant la vraie puce, depuis la page servie, dans un navigateur personnel connecté. **Claude reste le seul non bouclé** : `/new?q=` redirige vers `/login` et `q` disparaît de l'URL visible, aucun des deux navigateurs testés n'ayant de session Claude. ⚠️ Piège de mesure : un navigateur automatisé tombe sur `/sorry/index` chez Google et sur les murs de connexion ailleurs. **Ne jamais conclure de là qu'un bouton est mort** : juger depuis un navigateur personnel connecté. `gemini.google.com/app?q=` est MORT (composer vide) : c'est bien `google.com/search?udm=50&q=` qu'il faut. Méthode complète : skill `barre-resume-ia`.
- **Recherche & briefs** : `_research/` (gitignoré) contient les briefs SERP data-driven, l'analyse concurrentielle (6 concurrents) et les 8 angles « news » à publier (`tendances-news.json`, synthèse dans `SYNTHESE-CONCURRENCE-NEWSROOM.md`). Pic de recherche local ×4-8 en septembre : publier avant fin août.

# Cocon « référencement IA » (GEO), créé le 07/08/2026

Quatrième cocon français, et quatrième pilier : `/referencement-ia/` (`src/content/pillars/referencement-ia.ts`, écrit à la main, pas d'ingest) plus le cocon `/conseils/referencement-ia/` (`src/lib/articles/categories.ts`). Pendant hreflang de `/en/ai-search-optimization/` : c'est la **seule paire dont les deux versions vendent la même prestation**, les autres répondant à des intentions différentes par langue.

**Pourquoi ce cocon existe.** Le site vendait le GEO en anglais et pas un mot en français, alors que c'est le français qui monte. Volumes France mesurés le 07/08/2026 via DataForSEO, en **médiane** des 6 derniers mois contre les 6 précédents (jamais la moyenne : un pic isolé à 90 500 sur « agence seo » en mai 2026 la détruit ; jamais le `search_volume_trend.yearly` de DataForSEO non plus, qui s'ancre sur un seul mois) :

| Requête | S2 2025 | S1 2026 | Variation | CPC |
|---|---|---|---|---|
| geo seo | 2 650 | 2 900 | +9 % | 4,09 € |
| agence geo | 1 300 | 1 600 | +23 % | 16,65 € |
| geo ia | 720 | 880 | +22 % | |
| seo ia | 435 | 590 | +36 % | 8,24 € |
| référencement ia | 320 | 480 | **+50 %** | 9,50 € |
| formation geo | 215 | 355 | +65 % | |
| consultant geo | 170 | 260 | +53 % | 10,98 € |

Sur la même période le **jargon anglais s'effondre** : llmo 235 → 90 (-62 %), llm seo 175 → 100 (-43 %), answer engine optimization 100 → 60 (-40 %), generative engine optimization 880 → 720 (-18 %). D'où la décision de vocabulaire : **le terme de tête est « référencement IA » en français**, « GEO » en glose entre parenthèses, et jamais LLMO ni AEO en balise. La réponse IA de Google sur « référencement ia » ouvre elle-même par « Le référencement IA, ou GEO (Generative Engine Optimization) » : c'est le moteur qui donne le terme.

**Deux vérifications d'intention, à ne pas refaire à l'aveugle.** « geo seo » et « agence geo » ont été sortis en SERP le 07/08/2026 avant d'être retenus, parce que « geo » peut vouloir dire géomètre ou géolocalisation : les 10 résultats organiques des deux requêtes sont bien du Generative Engine Optimization. En revanche « geo france » (480/mois) avait été écarté le 30/07/2026 sur la même vérification, pour intention géographique.

**Ce que le pilier ne fait PAS.** Il ne vise pas « agence geo » de face : cette SERP est tenue par Eskimoz, Noiise, CyberCité et Orange Business. Il vise « référencement IA », dont la page 1 était occupée le 07/08/2026 par FranceNum et des blogs, **sans une seule page transactionnelle d'agence**.

**Articles : les 5 du cocon sont PUBLIÉS**, tous le 07/08/2026, en `category: "referencement-ia"` (via `_content-staging/` + `node scripts/ingest-content.mjs`). Le cocon est complet côté rédaction : la prochaine étape est une mesure (positions, part de voix IA), pas un article de plus.

| Slug | Titre servi | Cible mesurée |
|---|---|---|
| `geo-seo-difference` | GEO et SEO : quelle différence, et lequel vous rapporte ? | « geo seo », 2 900/mois |
| `etre-cite-par-chatgpt` | Comment être cité par ChatGPT, Perplexity et Gemini ? | « référencement chatgpt », 215/mois |
| `autoriser-robots-ia` | GPTBot, ClaudeBot : faut-il autoriser les robots des IA ? | le robots.txt réellement servi, piège Cloudflare |
| `llms-txt-a-quoi-ca-sert` | llms.txt : à quoi ça sert vraiment, et faut-il en faire un ? | « llms.txt », et le débat fichier miracle contre gadget |
| `mesurer-visibilite-ia` | Outils GEO : comment mesurer si les IA parlent de vous | « visibilité ia », « consultant geo » 260/mois |

⚠️ Cette liste a été laissée en « à écrire » pendant que les cinq articles étaient en ligne. **Vérifier `src/content/articles/` et le sitemap avant de croire un état rédactionnel écrit ici.**

`llms-txt-a-quoi-ca-sert` a été enrichi le 08/08/2026 d'un constat **mesuré** qui remplaçait une déduction tirée de la documentation : la catégorie Lighthouse « Agentic Browsing » note réellement un audit `llms-txt` (mesuré en Chrome 151, Lighthouse 13.4.1), et comme les 3 audits WebMCP de cette catégorie sortent en `notApplicable` avec un poids de 0, **ce fichier pèse un tiers du seul score applicable**. Méthode et pièges : skill `preparer-site-agents-ia`.

**Maillage.** Mesuré après création avec `carte-maillage.js` : le pilier ne recevait que 2 liens contextuels, tous deux de son propre cocon. Un lien depuis `seo-artisan` (section « Et demain : être recommandé par ChatGPT ») a été ajouté le 07/08/2026. Restent candidats naturels : `referencement-local` et `audit-seo`. Contrôle : `node ~/.claude/skills/maillage-interne/scripts/carte-maillage.js ./out --cible /referencement-ia/` (depuis PowerShell : Git Bash convertit l'argument en chemin Windows).

**Navigation.** « Témoignages » (ancre vers une section de l'accueil) a laissé sa place au pilier dans la barre de navigation. « Tarifs » ajouté en 6e position le 20/08/2026 : la barre est à **7 entrées**, re-mesurée ce jour-là à 1 280 px (une seule ligne, 105 px d'air avant le bloc téléphone/CTA) et à 375 px (burger, cible 44 px, zéro débordement).

# Outil d'audit SEO + IA (/audit-seo/), créé le 21/08/2026

Machine à leads en libre-service : scan gratuit sans inscription, 17 mesures
réelles faites sur le site du visiteur au moment du test, score sur 100
(70 points actifs + 30 d'autorité « à venir »), capture d'email contre le
rapport complet. Cadrage et décisions : dossier `Projet/Seo-referencement/`
(`cadrage-mvp-outil-audit-2026-08-21.md`).

- **Échelle commerciale à ne pas casser** : scan gratuit -> rapport gratuit
  envoyé PAR MICKAËL sous 24 h (léger : détail du scan + 3 priorités + un
  aperçu IA) -> audit complet PAYANT de `/tarifs/` (490 €, 690 € avec la
  mesure des citations IA). Le rapport gratuit ne contient jamais la mesure
  complète des citations : c'est le produit payant.
- **Fichiers** : moteur `functions/api/_engine.mjs` (portable Workers et
  Node >= 18, zéro dépendance), route `functions/api/scan.js` (POST /api/scan,
  une phase par appel : origin, robots, page, notfound), front
  `src/components/audit/` (AuditScan interactif + AuditContent gabarit),
  pages `(fr)/audit-seo/` et `(en)/en/seo-audit/` (conversion assumée, volumes
  EN mesurés à zéro le 21/08/2026), textes du chrome dans `ui.audit` (i18n).
- **Sondes = code de production** : harnais de vérité terrain
  `node scripts/test-audit-engine.mjs --attendu-mkz` (mesuré vert le
  21/08/2026, 69/70, seul manque HSTS). Toute évolution du moteur repasse par
  le harnais AVANT d'être crue. Chaque check mesure et sait répondre « non
  mesurable » ; jamais d'estimation.
- **Leads** : Web3Forms (la clé du formulaire de contact), sujet préfixé
  « Lead audit SEO : », consentement RGPD par case non pré-cochée (les deux
  politiques de confidentialité décrivent la collecte depuis le 21/08/2026).
  Pas d'envoi automatique du rapport : choix du cadrage, l'humain convertit.
- **Déploiement** : `npx wrangler pages deploy out` bundle AUSSI le dossier
  `functions/` (cwd = racine du dépôt dans deploy.mjs). Invariant à mesurer
  après chaque déploiement : POST https://mkz-consulting.fr/api/scan
  {"phase":"origin","url":"mkz-consulting.fr"} répond 200 avec une origine.
  Test local complet : `npx wrangler pages dev out` puis la page /audit-seo/.
- **DÉPLOYÉ le 21/08/2026**, recette `livraison-web` complète : pré-publication
  0 erreur, passe mobile 375 px mesurée au DOM (0 débordement avant ET après
  scan, cibles 52 px), live 0 erreur (miroir pages.dev en 302 Access, la seule
  ATTENTION est le faux positif Content-Signal documenté plus haut), sitemap
  48 URLs resoumis, « Indexation demandée » CONFIRMÉE sur modale visible pour
  /audit-seo/ et /en/seo-audit/ le jour même.
- ⚠️ **Piège d'auto-scan, payé au premier déploiement** : depuis le Worker, un
  fetch `http://` vers SA PROPRE zone part à l'origine sans la redirection de
  bord : http répondait 200 en 0 saut vu du Worker (301 vu de l'extérieur),
  d'où un faux « site accessible en double » sur mkz-consulting.fr. Corrigé :
  `phaseOrigin(url, self)` ne teste que les variantes https quand la cible est
  l'hôte de l'outil, et le détail affiche « variantes http non testables en
  auto-scan ». Ne pas « simplifier » ce paramètre.
- **Mise en avant FAITE le 21/08/2026** (déployée, mesurée sur le live) :
  l'entrée footer « Audit gratuit » (groupe Services) pointe désormais vers
  `/audit-seo/` au lieu de Calendly, dans les deux langues (Calendly reste
  servi par le CTA du haut du footer et celui du header) ; la barre de nav à
  7 entrées n'a PAS été touchée. Maillage contextuel mesuré au build par
  `carte-maillage.js` : **7 liens entrants** vers `/audit-seo/` (accueil ×2,
  piliers referencement-seo et referencement-ia, articles `audit-seo` et
  `mesurer-visibilite-ia` ; ces deux articles sont édités dans le `.ts` ET
  dans `_content-staging/`, dateModified 2026-08-21) et **4 liens** vers
  `/en/seo-audit/` (accueil EN ×2, piliers french-seo et
  ai-search-optimization). Les deux pages outil ne sont plus des culs-de-sac :
  bloc « Pour aller plus loin » / « Keep reading » (`nextLinks` dans
  `AuditContent`, 4 sortants FR, 3 EN). Les trois derniers culs-de-sac ont été
  réglés le jour même (deuxième passe, déployée et mesurée : « aucun
  cul-de-sac », 328 liens contextuels) : /contact/ et /en/contact/ via la
  cellule `explore` de la pile latérale (champ `ui.contact.explore`, i18n),
  /empreinte-ia/ via le bloc `.suite-liens` en fin de section FAQ, édité DANS
  LE PROJET SOURCE (Projet/Mon empreinte ia, template + rebuild + copie dans
  public/empreinte-ia/, jamais la copie seule). ⚠️ Piège de sonde payé sur
  cette passe : carte-maillage déclarait /empreinte-ia/ toujours cul-de-sac
  parce qu'un commentaire CSS contenait « <footer » en toutes lettres, que son
  strip regex avalait 37 Ko de corps depuis le head ; sonde durcie
  (commentaires, style et script retirés d'abord) et leçon consignée dans le
  skill maillage-interne.
- **Reste à faire (S2 à S4 du cadrage)** : Tier 1 DataForSEO (autorité, 30
  points, avec plafond quotidien dur et clé en secret Worker), Turnstile et
  limites par IP, et la 301 seo-referencement.fr vers la page (hors périmètre
  du dépôt : zone OVH du domaine).

# Témoignages clients (accueil, pilier agence-web-77, llms-full)

Cinq personas (Marc T. plombier à Meaux, Sophie L. architecte d'intérieur à Paris, Pierre D. restaurateur à Dammartin, Caroline B. coach à Chelles, Anne-Laure M. photographe à Melun) qui vivent à **quatre endroits** : `testimonials` dans `src/components/HomeContent.tsx` (les trois premiers), `src/app/llms-full.txt/route.ts` (les cinq), et les blocs `quote` de `src/content/pillars/agence-web-77.ts` (Pierre, Marc, Anne-Laure, Caroline), **à reporter aussi dans `_content-staging/agence-web-77.json`** sous peine de perte au prochain ingest. Refonte du 21/08/2026 (commits `dc7f930` puis `3ebfa9d`) :

- **Gabarit** : trois blocs identiques (Fraunces italique `clamp(19px, 1.7vw, 22px)`, filet, attribution, étiquette résultat), 3 colonnes à partir de 1 024 px, empilés en dessous. La « pull quote magazine » de 40 px suivie de deux citations en 15,5 px a été retirée : le lecteur voyait « un très gros, un invisible ». Invariant mesuré à 1 280 px : même corps, même largeur, même ligne de départ ; les hauteurs diffèrent, c'est voulu.
- **Écriture** : une voix par personne (registre, attaque, longueur, tics), sinon « on a l'impression que c'est la même personne qui les a écrits ». **« Mickaël » n'est nommé que dans UN témoignage sur les cinq** (Pierre), les autres disent MKZ, « on », ou rien. Jamais « l'équipe MKZ » (le site dit « Je décroche »). Chiffres modestes, un détail concret chacun, un bémol chez Caroline. Contrôle rejouable : premiers mots tous différents, occurrences de « Mickaël » = 1 sur les cinq textes.
- **Réserve** : ce sont des personas. À remplacer par de vrais retours dès qu'il y en a trois (un faux avis est une pratique commerciale trompeuse). Le rédactionnel ne passe jamais sur la GBP.

# Polices et stabilité visuelle

Trois familles web auto-hébergées dans `public/fonts/`, déclarées dans `src/lib/GlobalStyles.tsx`, piles dans `src/lib/theme.ts` : Fraunces en titrage, Archivo en corps, IBM Plex Mono en accent.

**Chaque famille est suivie d'un repli « X Fallback » aux métriques ajustées** (`size-adjust`, `ascent-override`, `descent-override`), posé le 08/08/2026. Ce n'est pas décoratif : sans lui, le H1 de l'accueil s'affichait sur 4 lignes en Georgia puis retombait à 3 en Fraunces, faisant remonter tout le hero de 43 px d'un coup. **CLS mesuré 0,1412 avant, 0,0012 après** (chargements froids, mobile, Chrome 151), et la catégorie Lighthouse « Agentic Browsing » est passée de 0,94 à 1,00.

Trois règles à ne pas casser :

- **Une seule face par famille de repli.** L'italique passe en oblique synthétique, c'est la configuration sous laquelle les valeurs ont été mesurées. Déclarer une face italique avec une autre police locale casse le calage.
- **Le repli vient JUSTE APRÈS la police web** dans la pile, avant les polices locales nues.
- **Les valeurs ne se calculent pas, elles se balaient.** Fraunces porte un axe de taille optique : un ratio de largeurs mesuré au canvas à 1000 px sort 76 % là où il faut 104 %, et le correctif AGGRAVE le CLS (payé le 08/08). Chaque valeur est le centre de l'intersection des plateaux relevés à 375 px **et** à 412 px, un plateau relevé sur une seule largeur ne disant rien de l'autre.

Refaire la mesure si une police, une graisse ou une pile de repli change : skill `cls-polices-web` (`caler-replis-polices.js` pour les valeurs, `sonde-cls.js` pour vérifier avant et après sur chargement froid). Le script projet qui faisait ce travail a été supprimé le 08/08 au profit du skill, qui détecte seul les familles.

**Limite assumée** : le calage vise Georgia, Segoe UI et Consolas, les polices du poste de mesure. Sur un système qui n'a aucune des trois (Android), le repli tombe sans ajustement et le CLS y reste celui d'avant.

# Pièges de build et de vérification

- **React 19 insère des marqueurs `<!-- -->` entre deux expressions JSX adjacentes.** Le HTML servi contient `Mis à jour le<!-- --> <!-- -->8 août 2026` et `9<!-- --> <!-- -->min de lecture`. Chercher la chaîne littérale « Mis à jour le 8 » répond donc « absent » alors que le texte s'affiche parfaitement. Toute vérification par sous-chaîne sur le HTML produit doit en tenir compte, sous peine de faux négatifs en série.
- **React échappe l'apostrophe en `&#x27;` dans le HTML statique.** `grep "J'avais" out/index.html` répond 0 sur une page qui affiche la phrase. Même famille que les `<!-- -->` : sonder sur un fragment sans apostrophe, ou comparer le `innerText` rendu. Payé le 21/08/2026 en vérifiant les témoignages.
- **`.next` peut bloquer le build en `EPERM: unlink`.** Tout le dossier est marqué point de reparse sans cible, la signature des fichiers à la demande OneDrive, et Next échoue à le nettoyer. `Remove-Item .next -Recurse -Force` débloque. Rien à voir avec le code, et ça revient.
- **`ingest-content.mjs` écrase les éditions faites à la main dans `src/content/articles/*.ts`.** L'en-tête des fichiers générés autorise l'édition manuelle (c'est même la procédure documentée pour ajouter une capture), mais l'ingest régénère depuis `_content-staging/` sans rien reprendre en sens inverse : toute édition non reportée dans le JSON source est perdue au prochain ingest, silencieusement. Payé le 16/08/2026 : un ingest lancé pour une correction d'ancre a supprimé les **17 captures d'écran** des 3 tutoriels et restauré 7 légendes obsolètes (« Commencer maintenant » à la place de « Ajouter un site Web », correction du 08/08). Le staging a été resynchronisé depuis les `.ts` le jour même, et l'ingest est redevenu idempotent. **Deux règles** : après toute édition manuelle d'un `.ts`, reporter le changement dans `_content-staging/<slug>.json` ; et avant tout ingest, contrôler la divergence, puis `git diff --stat src/content/` juste après, en restaurant les fichiers non voulus (`_content-staging/` est gitignoré, donc sans filet git : le sauvegarder avant d'y écrire). Invariant à mesurer, jamais le proxy : un second ingest à vide ne doit produire aucun diff, et `grep -c '"src": "/images/conseils/'` doit rendre 7, 4 et 6 sur les trois tutoriels.

# Déploiement

- Hébergement : **Cloudflare Pages** (projet `mkz-site`, compte Mkzcons@gmail.com). Production : `https://mkz-consulting.fr` (+ `mkz-site.pages.dev`).
- `npm run deploy:build` = build statique (`out/`) + direct upload via wrangler (récupéré par npx, pas de dépendance npm). Requiert `CLOUDFLARE_API_TOKEN_MKZ` (fallback `CLOUDFLARE_API_TOKEN`) + `CLOUDFLARE_ACCOUNT_ID` en variables d'environnement.
- **Après `npm run deploy`, la première requête peut encore servir l'ancienne version pendant quelques secondes** (mesuré le 21/08/2026 : `llms-full.txt` en version précédente, puis identique au build deux requêtes plus tard, `cf-cache-status: DYNAMIC`, `max-age=0`). Ce n'est pas un cache : attendre une dizaine de secondes, puis comparer `llms.txt`, `llms-full.txt`, `sitemap.xml` et `robots.txt` au build avec `cmp`, jamais par sous-chaîne.
- En-têtes HTTP : `public/_headers`. Redirection www→apex : règle de redirection **dans la zone Cloudflare** (pas de `_redirects`, Pages ne supporte pas les redirections inter-hôtes). Pas de `.htaccess` (l'hébergement OVH est abandonné depuis juin 2026). **HSTS + CSP ajoutés le 21/08/2026** sur inventaire mesuré (commentaires dans le fichier) : HSTS sans includeSubDomains (sous-domaines mail OVH sur la zone), CSP avec 'unsafe-inline' (scripts inline Next + styles Emotion), cdn.hu-manity.co, *.clarity.ms, c.bing.com et api.web3forms.com. ⚠️ Toute nouvelle ressource externe (script, iframe, fetch) doit être ajoutée à la CSP, sinon elle est bloquée en silence : contrôler la console navigateur après tout ajout d'intégration.
- **`mkz-site.pages.dev` en noindex depuis le 20/08/2026.** Le sous-domaine technique servait une copie complète du site en 200 indexable (0 URL indexée par Google, mesuré le jour même, mais doublon citable par les moteurs IA). Deux blocs limités à l'hôte dans `public/_headers` (recette documentée Cloudflare, placeholders `:project` et `:version`) posent `X-Robots-Tag: noindex` sur `mkz-site.pages.dev` ET sur les URLs de déploiement horodatées. **Fermé aussi aux humains le 21/08/2026** : application Access `1c25a6de-0420-4f95-85cc-ebaafb4b669d` (compte Zero Trust `shrill-voice-5ee6.cloudflareaccess.com`, nom d'équipe auto-généré, renommable dans Zero Trust → Settings) sur `mkz-site.pages.dev` + `*.mkz-site.pages.dev`, politique allow sur mkzcons@gmail.com (code par mail). Créée par API (token MKZ élargi à `Access: Apps and Policies Edit` le 21/08). Le noindex de `_headers` reste en place en défense en profondeur. **Invariant mesuré après chaque déploiement** : `verify-livraison.js https://mkz-consulting.fr --miroir https://mkz-site.pages.dev` (le miroir redirigé vers cloudflareaccess.com, l'apex en 200 SANS `X-Robots-Tag`, sinon GSC répond BLOCKED_BY_HTTP_HEADER). Le déploiement wrangler n'est pas concerné (il passe par l'API, pas par les hôtes pages.dev).
- **Crawlers IA débloqués le 11/06/2026** (stratégie GEO-first) : `ai_bots_protection=disabled` + `is_robots_txt_managed=false` sur la zone Cloudflare : c'est le `public/robots.txt` du repo qui est servi (Allow crawlers IA + Content-Signals tout à yes). **Ne pas réactiver le blocage IA Cloudflare sans décision explicite de Mickaël** : ça remettrait tous les bots IA en 403 et écraserait le robots.txt par la version managée (Disallow GPTBot/ClaudeBot/…).
- **Microsoft Clarity installé le 05/08/2026** (projet `xxtffll8xk`), **uniquement derrière le consentement** : chargeur `src/components/ClarityScript.tsx`, rendu par les deux root layouts. Ne JAMAIS coller le snippet Clarity officiel tel quel : en export statique, un script inline s'exécute avant que l'autoblocking du bandeau hu-manity (afterInteractive) soit actif, Clarity partirait sans consentement. Le chargeur n'injecte le tag qu'une fois la catégorie 4 du bandeau accordée (cookie `hu-consent`, événement `set-consent.hu` sur document), envoie `clarity('consentv2', ...)` à l'injection (exigé en EEE depuis le 31/10/2025 : sans ce signal, mode dégradé sans cookie, mesuré) et appelle `clarity('consent', false)` au retrait (efface `_clck`/`_clsk`). Les pages légales FR et EN décrivent Clarity (4e sous-traitant, section Cookies) : toute évolution du dispositif de mesure se répercute dans les deux langues. Flux vérifié en prod le 05/08/2026 : rien sans choix, cookies posés sur acceptation, effacés au retrait.
- **Google Analytics 4 installé le 21/08/2026** (propriété 480932780, flux web 10345084267 réutilisé, ID de mesure `G-YE680NFBYZ`), **uniquement derrière le consentement**, par le chargeur `src/components/GaScript.tsx`, copie conforme du mécanisme Clarity (cookie `hu-consent`, événement `set-consent.hu`, catégorie 4), rendu par les deux root layouts juste après `ClarityScript`. Consent Mode v2 posé à l'injection (analytics_storage granted, tout le publicitaire denied, Google Signals off), cookies `_ga` et `_ga_*` limités à 13 mois (`cookie_expires: 33696000`, plafond CNIL). Retrait : `consent update` denied + `window["ga-disable-G-YE680NFBYZ"] = true` + effacement des `_ga*` sur l'hôte et le domaine nu. CSP de `public/_headers` élargie à `www.googletagmanager.com` (script, img, connect) et `*.google-analytics.com` + `*.analytics.google.com` (connect, img : les hits partent sur `region1.google-analytics.com/g/collect` depuis la France). Pages légales FR (confidentialité + mentions) et EN (privacy + legal notice) décrivent GA4 comme 5e sous-traitant (Google Ireland Limited, transfert possible vers Google LLC, DPF). Décidé après le check UX du 21/08/2026 (`_ux/check-ux-2026-08-21.md`) : le site n'avait aucune donnée de comportement, la propriété GA4 existait depuis mars 2025 mais le tag n'avait jamais été posé sur le site Cloudflare (0 `gtag` servi, 4 sessions en 6 mois, reliquat OVH). Test local avec CSP : `npx wrangler pages dev out --port 8788` (entrée `mkz-wrangler` de `.claude/launch.json`), `http.server` ne sert pas `_headers`. Vérifié en local le 21/08/2026 sur `wrangler pages dev` (CSP active) : 0 script et 0 cookie avant consentement ; après acceptation `google_tag_manager["G-YE680NFBYZ"]` initialisé, `_ga` + `_ga_YE680NFBYZ` posés, dataLayer = consent default, js, config ; consentement mémorisé d'une page à l'autre ; au retrait `consent update denied`, `ga-disable` à true, cookies `_ga*` effacés. Au passage, le pixel Clarity `c.bing.com/c.gif` était bloqué par la CSP (déclaré en connect, pas en img) : corrigé le même jour. **Recette prod après chaque déploiement** : accepter le bandeau réel, vérifier que `performance.getEntriesByType("resource")` contient un hit `google-analytics.com/g/collect`, puis `run_realtime_report` (activeUsers) sur la propriété : c'est le hit reçu qui prouve l'installation, pas le script présent. **Déployé et mesuré en prod le 21/08/2026** (déploiement `fae8e392`) : avant consentement 0 script, 0 `dataLayer`, 0 requête Google ; après « Sauvegarder mes choix » avec la catégorie 4 cochée sur le VRAI bandeau : `gtag/js` chargé, hit `region1.google-analytics.com/g/collect` parti, cookies `_ga` + `_ga_YE680NFBYZ` posés, **1 utilisateur actif dans le temps réel GA4** (page accueil, France), 0 violation CSP, consentement mémorisé sur /contact/. ⚠️ Deux faits mesurés au passage. (1) Le cookie `hu-consent` réel est du JSON BRUT (`{"consent":true,"consentLevel":1,"consentID":...,"categories":{...}}`), pas encodé : un cookie fabriqué avec `encodeURIComponent` fait planter le bandeau (« Unexpected token % »). (2) Une erreur console `E18 Unexpected server data: missing bootstrap script` (pile dans `hu-banner.min.js`) apparaît à CHAQUE sauvegarde du bandeau, y compris avec les seules catégories 2 et 3 cochées, GA4 et Clarity inertes : c'est le bandeau qui ré-exécute des scripts inline de Next à la sauvegarde, pas le chargeur GA4. Antérieur à GA4, sans effet visible sur la page, à régler côté hu-manity (autoblocking) si on veut une console propre. **Question ouverte** : le bandeau étiquette la catégorie 4 « Pub personnalisée » alors qu'elle porte Clarity et GA4 (classification de la table vendors hu-manity, mesurée le 05/08) ; un visiteur qui refuse « la pub » coupe la mesure d'audience, et la catégorie 3 « Site optimisé » serait le libellé honnête. À trancher avec hu-manity avant de lire les taux de consentement.
- **MCP Clarity déclaré le 06/08/2026** (serveur officiel `@microsoft/clarity-mcp-server` v2.0.1), en **portée projet** dans `~/.claude.json`, lancé par `cmd /c npx -y @microsoft/clarity-mcp-server --clarity_api_token=%CLARITY_API_TOKEN_MKZ%`. La clé (JWT Data.Export du projet `xxtffll8xk`, copie locale dans le `.env` gitignoré du dossier) vit dans la variable d'environnement utilisateur **`CLARITY_API_TOKEN_MKZ`**, jamais en clair dans la config : un `command: "npx"` direct échoue sous Windows, et un token en argument part en clair dans le fichier (deux essais de ce type retirés le 06/08, nettoyage rejouable : `node %USERPROFILE%\.claude\backups\nettoie-clarity-racine.js`). Clé validée contre l'API le 06/08 (HTTP 200, données présentes). Outils : `query-analytics-dashboard`, `list-session-recordings`, `query-documentation-resources`, visibles au prochain démarrage de session. ⚠️ **Quota API Data Export : 10 requêtes par projet et par jour, 3 jours de données, 3 dimensions par requête** : préparer ses questions, pas d'exploration à tâtons. Même dispositif sur TrouveTonAppart avec `CLARITY_API_TOKEN_TTA` (une clé Clarity par projet).
- **Ouverture aux robots IA : état MESURÉ le 15/08/2026, pas déduit.** Le déclaratif était déjà bon, il a été vérifié sur l'effet et non sur le correctif. Trois relevés, tous rejouables :
  1. **Accès réel** : 20 User-Agents de robots (GPTBot, OAI-SearchBot, ChatGPT-User, ClaudeBot, Claude-User, Claude-SearchBot, PerplexityBot, Perplexity-User, Googlebot, Google-CloudVertexBot, Bingbot, Applebot, meta-externalagent, Amazonbot, CCBot, DuckAssistBot, MistralAI-User, Bytespider, cURL nu, navigateur) × 9 URLs = **180 requêtes, 180 réponses 200, aucun `X-Robots-Tag`**. C'est ça, l'invariant : « robots.txt dit Allow » n'est que le proxy. Le `robots.txt` servi est **identique octet pour octet** à `public/robots.txt`, en `text/plain` : la preuve que le managé Cloudflare n'est pas actif. ⚠️ `verify-livraison.js` lève une ATTENTION « Content-Signal présent, robots.txt managé probablement actif » : **c'est un faux positif sur ce site**, le Content-Signal est le nôtre et il est permissif.
  2. **Couverture d'exploration** : croisement de quatre ensembles (pages publiées dans `out/`, `<loc>` du sitemap, URLs atteignables en suivant les liens depuis l'accueil, URLs citées dans `llms.txt`). Résultat : **45 URLs au sitemap, 45 atteignables, 0 orpheline, 0 lien cassé, 0 cul-de-sac**. Seule `/_not-found/` est publiée hors sitemap et sans lien entrant : artefact Next, elle porte un `noindex` et les vraies 404 sortent bien en 404, donc aucun soft-404.
  3. **Lighthouse Agentic Browsing** : **1,00** sur l'accueil, Chrome 151, mobile. `agent-accessibility-tree` 1, `cumulative-layout-shift` 0, `llms-txt` 1 ; les 3 audits WebMCP en `notApplicable`, poids 0. Relevé exact de l'arbre d'accessibilité sur 5 gabarits (accueil, contact, article, pilier EN, empreinte-ia) : **tous les éléments interactifs portent un nom accessible**.
  ⚠️ **Piège de sonde payé ici** : tester l'appartenance d'un lien au domaine par `href.includes("mkz-consulting.fr")` fabrique 3 faux liens cassés, parce que les liens de la barre « Résumer avec l'IA » portent l'URL canonique en paramètre (`claude.ai/new?q=https://mkz-consulting.fr/...`). **Comparer le hostname, jamais par sous-chaîne.**
  **Indexation contrôlée le 15/08/2026** sur les 6 hubs : 5 en « Submitted and indexed », seul `/en/insights/ai-search/` en « Discovered - currently not indexed », pour lequel une demande d'indexation a été faite et **confirmée** (modale « Indexation demandée », lue sur l'élément visible). Passe mobile 375 px sur 3 gabarits après redéploiement : **0 px de débordement horizontal** partout.
  Défaut réellement trouvé et corrigé le 15/08/2026 : les **4 hubs de cocon français et les 2 anglais** étaient au sitemap et atteignables par les liens, mais **absents de `llms.txt` et `llms-full.txt`**. Un modèle qui ne lit que ces fichiers voyait les articles sans jamais voir la page d'entrée de leur cocon. Les titres de section de `llms.txt` sont désormais le lien vers le hub, et les listes de pages de `llms-full.txt` sont générées depuis le registre : un cinquième cocon s'y ajoutera tout seul.
- **Obfuscation e-mail désactivée le 30/07/2026** (`email_obfuscation=off`, zone `97f939b5ac384f6d885eedacb1da4777`). Elle était sur `on`, la valeur par défaut de Cloudflare, jamais modifiée. Scrape Shield réécrivait chaque `mailto:` en `<span class="__cf_email__">[email protected]</span>` + script de décodage, **sur les 10 pages du site**. Effet mesuré : un crawler sans JavaScript ne lisait pas l'adresse, alors que la LCEN impose un moyen de contact et que la stratégie GEO vise justement la citabilité. **Ne pas réactiver sans décision explicite de Mickaël.** Piège à connaître : le défaut est invisible depuis le dépôt et depuis `*.pages.dev`, la transformation n'ayant lieu que sur la zone. Contrôle : `node scripts/verify-coordonnees.mjs https://mkz-consulting.fr` (ou chercher `__cf_email__` dans le HTML servi).

# Liens sortants : qui garde son dofollow

Règle posée le 19/08/2026, après mesure : **les outils de contact sortent en `nofollow`, les sources citées gardent leur dofollow.** Un CTA n'est pas une citation.

Le défaut ne venait pas des articles mais du **gabarit** : `Footer.tsx` rendait le lien de nav « Audit gratuit » sans `nofollow`, donc **44 liens suivis vers calendly.com** sur les 44 pages qui portent un pied de page. `Button.tsx` faisait déjà le bon geste depuis le 08/08/2026, mais trois endroits ne passent pas par lui : la nav du pied de page, le `MobileCTA` du `Header` (dans un bloc conditionnel, donc absent du HTML statique et responsable de 0 lien, corrigé par cohérence) et les liens vers le DPA de Calendly des deux pages légales. Mesure avant/après : **64 liens externes suivis → 18**, calendly.com de 46 suivis à 0.

**Restent en dofollow, volontairement** : `pagespeed.web.dev`, `search.google.com` et `business.google.com` (outils cités dans les tutoriels), `cnil.fr` (autorité), `maps.app.goo.gl` (fiche Google Business de MKZ, citation locale utile). **Question laissée ouverte** : dans la même phrase des pages légales, les DPA de `cloudflare.com` (4 liens), `web3forms.com` (2) et `privacy.microsoft.com` (2) sont restés suivis alors que celui de Calendly est passé en nofollow ; ce sont pourtant la même chose, des mentions de sous-traitants imposées par le RGPD. À trancher.

⚠️ **Sonde obligatoire : comparer le HOSTNAME, jamais une sous-chaîne.** Les liens de la barre « Résumer avec l'IA » portent l'URL canonique de la page en paramètre, donc un test par sous-chaîne les compte comme externes (ou comme internes selon le sens du test). Invariant à mesurer sur `out/`, pas sur les composants : aucun lien vers `calendly.com` ni `wa.me` ne doit sortir sans `nofollow`.
